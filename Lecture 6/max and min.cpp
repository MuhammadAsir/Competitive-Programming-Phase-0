#include<iostream> // built in function max and min
using namespace std;

int main() {
    int a = 15, b = 20;
    cout << "Max of a and b: " << max(a, b) << '\n'; // prints 20
    cout << "Min of a and b: " << min(a, b) << '\n'; // prints 15
    return 0;
}
